const CONFIG={whatsappNumber:'5555999834112',whatsappMessage:'Olá! Vi o site da Fatta Veículos e gostaria de saber mais sobre os veículos disponíveis.',whatsappDisplay:'(55) 99983-4112'};
function buildWhatsAppLink(){const text=encodeURIComponent(CONFIG.whatsappMessage);return `https://wa.me/${CONFIG.whatsappNumber}?text=${text}`}
function wireWhatsAppLinks(){const link=buildWhatsAppLink();['wa-header','wa-hero','wa-location','wa-cta','wa-fab'].forEach(id=>{const el=document.getElementById(id);if(el)el.setAttribute('href',link)});const display=document.getElementById('wa-display');if(display)display.textContent=CONFIG.whatsappDisplay;const footer=document.getElementById('wa-footer-text');if(footer)footer.textContent=`WhatsApp: ${CONFIG.whatsappDisplay}`}
function wireMobileNav(){const toggle=document.getElementById('navToggle');const nav=document.getElementById('mobileNav');if(!toggle||!nav)return;toggle.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('is-open');toggle.setAttribute('aria-expanded','false')}))}
function highlightToday(){const rows=document.querySelectorAll('.hours-table tbody tr');if(!rows.length)return;const d=new Date().getDay();const i=d===0?6:d-1;if(rows[i])rows[i].classList.add('is-today')}
function setFooterYear(){const el=document.getElementById('year');if(el)el.textContent=new Date().getFullYear()}
function repairGallery(){document.querySelectorAll('#vitrine .gallery-grid img').forEach((img,index)=>{img.src=`/assets/img/veiculo-${String(index+1).padStart(2,'0')}.webp`});const style=document.createElement('style');style.textContent='.photo-slot,.photo-slot--light{border:none!important}.gallery-grid .photo-slot{background:var(--color-paper-dim)!important}';document.head.appendChild(style)}
function wireScrollReveal(){if(!('IntersectionObserver'in window)||window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const items=document.querySelectorAll('#sobre .photo-slot,#sobre .about-copy,#diferenciais .section-head,#diferenciais .tag,#vitrine .section-head,#vitrine .photo-slot,#vitrine .gallery-note,#localizacao .location-grid>* ,.final-cta .container>* ,.site-footer .footer-grid>*');items.forEach(el=>el.classList.add('reveal'));const obs=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');obs.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -6% 0px'});items.forEach(el=>obs.observe(el))}
document.addEventListener('DOMContentLoaded',()=>{repairGallery();wireWhatsAppLinks();wireMobileNav();highlightToday();setFooterYear();wireScrollReveal()});